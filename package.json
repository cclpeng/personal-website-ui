{
  "name": "first-spa",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^1.6.0",
    "bootstrap": "^4.6.0",
    "dotenv": "^9.0.0",
    "jquery": "^3.6.0",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-router-dom": "^5.2.0",
    "react-scripts": "3.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "start:development": "Set REACT_APP_ENV=development&&npm start",
    "build:development": "Set REACT_APP_ENV=development&&npm run build",
    "start:production": "Set REACT_APP_ENV=production&&npm start",
    "build:production": "Set REACT_APP_ENV=production&&npm run build",
    "Note": "to start a script, the command is 'npm run script-name', eg. 'npm run start:development'"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
